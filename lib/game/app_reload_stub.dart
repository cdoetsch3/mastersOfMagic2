/// Off the web (tests, and the Android/iOS shells) there is no page to
/// reload — the gate screen still blocks, the button just has nothing to do.
/// See `app_reload.dart` for why this is a conditional import.
void reloadApp() {}
